# TUPLES - IMMUTABLE

numbers = (1, 2, 3, 1) # TUPLE
# numbers[0] = 100 # ERROR
# print(numbers[0])

# numbers.count(3)
print(numbers.count(1)) # number of occurences
print(numbers.index(3)) # index of the first occurence
